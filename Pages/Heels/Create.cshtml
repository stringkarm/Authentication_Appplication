@page
@model Authentication_Appplication.Pages.Heels.CreateModel

@{
    ViewData["Title"] = "New Collection | ÉLÉGANCE";
    Layout = "/Pages/Shared/_Layout.cshtml";
}

<style>
    .create-wrapper {
        padding: 80px 20px;
        background-color: #f9f9f9;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .create-card {
        background: white;
        width: 100%;
        max-width: 550px;
        padding: 50px;
        box-shadow: 0 20px 60px rgba(0,0,0,0.05);
        border: 1px solid #eee;
    }

    .create-header {
        text-align: center;
        margin-bottom: 40px;
    }

        .create-header h1 {
            font-family: 'Playfair Display', serif;
            font-size: 32px;
            text-transform: uppercase;
            letter-spacing: 3px;
            margin-bottom: 10px;
        }

        .create-header p {
            font-size: 11px;
            color: #888;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

    .form-group-lux {
        margin-bottom: 30px;
    }

    .control-label-lux {
        font-size: 11px;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 1px;
        margin-bottom: 8px;
        display: block;
        color: #333;
    }

    .form-control-lux {
        width: 100%;
        padding: 12px 0;
        border: none;
        border-bottom: 1px solid #ddd;
        border-radius: 0;
        font-size: 14px;
        transition: border-color 0.3s;
        outline: none;
        background: transparent;
    }

        .form-control-lux:focus {
            border-bottom: 1px solid #c9a050;
        }

    textarea.form-control-lux {
        min-height: 120px;
        border: 1px solid #eee;
        padding: 15px;
        margin-top: 5px;
        line-height: 1.6;
    }

        textarea.form-control-lux:focus {
            border: 1px solid #c9a050;
        }

    .btn-create-lux {
        background-color: #000;
        color: #fff;
        width: 100%;
        padding: 20px;
        font-size: 13px;
        font-weight: 800;
        letter-spacing: 4px;
        text-transform: uppercase;
        border: none;
        cursor: pointer;
        transition: all 0.3s ease;
        margin-top: 10px;
    }

        .btn-create-lux:hover {
            background-color: #c9a050;
            transform: translateY(-2px);
        }

    .back-link {
        margin-top: 30px;
        font-size: 10px;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 2px;
        color: #999;
        text-decoration: none;
        transition: color 0.2s;
    }

        .back-link:hover {
            color: #000;
        }

    .required-star {
        color: #c9a050;
        margin-left: 3px;
    }
</style>

<div class="create-wrapper">
    <div class="create-header">
        <h1>New <span style="color:#c9a050">Collection</span></h1>
        <p>Introduce a Masterpiece to the Archive</p>
    </div>

    <div class="create-card">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger" style="font-size: 12px; margin-bottom: 20px;"></div>

            <div class="form-group-lux">
                <label asp-for="Heel.Name" class="control-label-lux">Heel Name<span class="required-star">*</span></label>
                <input asp-for="Heel.Name" class="form-control-lux" placeholder="e.g. The Midnight Stiletto" />
                <span asp-validation-for="Heel.Name" class="text-danger" style="font-size: 11px;"></span>
            </div>

            <div class="form-group-lux">
                <label asp-for="Heel.Brand" class="control-label-lux">Brand<span class="required-star">*</span></label>
                <input asp-for="Heel.Brand" class="form-control-lux" placeholder="e.g. ÉLÉGANCE PARIS" />
                <span asp-validation-for="Heel.Brand" class="text-danger" style="font-size: 11px;"></span>
            </div>

            <div class="form-group-lux">
                <label asp-for="Heel.Description" class="control-label-lux">Description<span class="required-star">*</span></label>
                <textarea asp-for="Heel.Description" class="form-control-lux" placeholder="Share the inspiration behind this silhouette..."></textarea>
                <span asp-validation-for="Heel.Description" class="text-danger" style="font-size: 11px;"></span>
            </div>

            <button type="submit" class="btn-create-lux">Add Stock</button>
        </form>
    </div>

    <a asp-page="Index" class="back-link">← Return to Archive</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}